Original prompt: Instalar Phaser 3 no client/web e implementar o sistema de movimentação grid-based para o hub overworld, fiel ao estilo Pokemon Essentials (RPG Maker XP). Criar `src/hub/HubScene.ts`, `src/hub/HubGame.tsx`, adicionar `HUB (test)` em `src/screens/MainMenuScreen.tsx`, validar com `npx tsc --noEmit` e escrever o resultado no shard.

- 2026-03-31: Substituída a cena de hub por uma implementação Phaser autocontida, com tilemap placeholder, colisão nas bordas, walk/run, facing e input buffer.
- 2026-03-31: Ajustado `HubGame` para montar `#hub-canvas` com tamanho fixo de 480x320 e ciclo de criação/destruição do `Phaser.Game`.
- 2026-03-31: `npx tsc --noEmit` passou em `client/web`.
- 2026-03-31: Validado no browser com Playwright em porta local dedicada; o hub abriu pelo menu, expôs `render_game_to_text`, e respondeu a walk/run sem erros de console.
- 2026-03-31: Revalidado manualmente nesta sessão em `http://127.0.0.1:4317` com Vite + Playwright; capturados screenshot/estado do hub em `.shard/test/playwright-output-4317` e assertivas extras de run, colisão e facing em `.shard/test/hub-run-check.json`.
- 2026-03-31: Inspecionado `client/project-genesis` como referência de assets reais; o item pertinente implementado no `client/web` foi a sincronização da title art original (`title.png` e `start.png`) e a troca da `TitleScreen` placeholder por uma composição baseada nesses arquivos.
- 2026-03-31: Smoke test visual da nova `TitleScreen` salvo em `.shard/test/title-smoke/shot-0.png`; o fluxo `Enter -> Main Menu -> HUB (test)` continuou válido no smoke test do hub.
- 2026-03-31: Atendido o pedido de puxar assets da pasta real do Essentials. A árvore útil no workspace era `pokemon-essentials-v21.1-gen9/Graphics` (a cópia `sources/pokemon-essentials-21.1` não continha `Graphics/`).
- 2026-03-31: Sincronizados para `client/web/public/assets/essentials/` o follower sheet `PIKACHU.png`, `UI/statuses.png`, `UI/Battle/icon_statuses.png` e todos os ícones de ball em `UI/Summary/`.
- 2026-03-31: `HubScene` passou a usar o follower sheet real de Pikachu do Essentials como player visual, preservando o grid movement, facing, walk/run e colisão.
- 2026-03-31: Sincronizada a árvore inteira `client/genesis/public/assets` para `client/web/public/assets`, cobrindo mapas, battlebacks, personagens, sprites de UI, runtime sprites e reference UI do projeto Genesis.
- 2026-03-31: O hub Phaser do `client/web` foi convertido para o lobby da Battle Tower usando o mapa `battle-tower-lobby-base.png`, o mesmo personagem `boy_run.png`, NPCs/portas do runtime e colisão interna baseada no layout existente em `client/genesis`.
- 2026-03-31: `MainMenuScreen` passou a expor a entrada `Battle Tower`, e `HubGame` foi ajustado para 640x480 para encaixar o layout do lobby.
- 2026-03-31: Auditada a cobertura de assets entre `client/genesis/public/assets`, `pokemon-essentials-v21.1-gen9/Graphics` e `client/web/public/assets`. O delta do Genesis já estava zerado.
- 2026-03-31: Copiados os 32 arquivos ainda ausentes do Essentials para `client/web/public/assets/essentials/`, cobrindo `battle-animations/`, os quatro extras em `characters/` e `pokemon/shadow/`. Reexecução da rotina de cópia retornou `copied=0`.
- 2026-03-31: O PC do hub foi realinhado ao comportamento do Pokémon Essentials usando `019_UI_PC.rb` e `017_UI_PokemonStorage.rb` como fonte de verdade. `PcMenu` agora abre `Organize Boxes / Withdraw Pokemon / Deposit Pokemon / See ya!`, e `StorageOverlay` usa os bitmaps reais de storage com o layout/fluxo aproximados do Essentials em vez do stub do Genesis.
- 2026-03-31: `pcStorageData.ts` foi enriquecido com `frontSpriteSrc`, tipos, habilidade, item, gênero e shiny para alimentar o painel esquerdo da storage como no Essentials; também ampliei os boxes para 40 entradas para espelhar o PC real.
- 2026-03-31: Validação local do PC/storage em `http://127.0.0.1:4318` com Playwright: abriu o PC no tile correto, confirmou menu `BILL'S PC`, `Organize` selecionou Venusaur, `Withdraw` elevou `Party: 3 -> 4`, `Deposit` devolveu `Party: 4 -> 3`, sem erros de console. Artefatos: `.shard/test/pc-storage-check.json`, `.shard/test/pc-storage-check.png`, `.shard/test/verify-pc-storage.cjs`.
- 2026-03-31: Investigado um regressão visual pós-port do PC. O app não quebrou no boot/menu/hub, mas a preview sprite da storage podia aparecer vazia porque o front sprite não terminava de carregar a tempo no dev server. `StorageOverlay` agora faz fallback imediato para o ícone ampliado enquanto o front sprite não estiver pronto, removendo a área vazia que dava aparência de app quebrado.
- 2026-03-31: Revalidado em `http://127.0.0.1:4319` com Playwright após o fallback do preview. O fluxo do PC continuou íntegro e a captura final passou a mostrar o preview visível na lateral esquerda. Artefatos atualizados: `.shard/test/pc-storage-check.json`, `.shard/test/pc-storage-check.png`.
- 2026-03-31: Segunda passada de UI no PC/storage após feedback do usuário. `StorageOverlay` foi recalibrado para os bitmaps reais com ícones menores (`0.58`), grade de box menos inflada, preview reduzido, retorno do label de faixa do box no topo e ocultação dos sprites da party quando o painel está fechado. `PcMenu` também foi compactado para uma janela mais próxima do visual do Essentials.
- 2026-03-31: `cd client/web && node_modules/.bin/tsc.cmd --noEmit` passou após o retoque visual.
- 2026-03-31: A validação visual automatizada desta passada ficou inconclusiva neste shell porque Vite e Playwright precisaram escapar do sandbox (`spawn EPERM`) e a execução escalada do browser travou. A funcionalidade do PC já tinha sido confirmada; esta rodada ainda pede inspeção visual manual.
- 2026-03-31: Correção dirigida pelo screenshot do usuário. `client/web/src/components/ui-pixel/PixelMenuList.tsx` foi compactado para caber de forma limpa nas janelas do PC e da Nurse, e `client/web/src/hub/PcMenu.tsx` voltou a ter uma janela grande o bastante para os quatro comandos sem clipping.
- 2026-03-31: `client/web/src/hub/StorageOverlay.tsx` deixou de misturar ajuda/feedback com o painel esquerdo do Pokémon. O texto de status saiu da lateral e passou para um painel `PixelFrame` dedicado no canto inferior direito, o que deve eliminar a sobreposição feia mostrada no screenshot.
- 2026-03-31: Novo ajuste após o segundo screenshot: o painel de ajuda local no canto ainda não parecia modal o bastante para `Deposit`. `client/web/src/hub/StorageOverlay.tsx` agora usa uma faixa `PixelFrame` de largura completa no rodapé da storage, com título do modo, status, contagem da party e controles no mesmo bloco, cobrindo a faixa inferior da tela em vez de deixar elementos vazando por baixo.
- 2026-03-31: Ajuste dirigido pelo terceiro screenshot: a faixa inferior inteira foi descartada. `client/web/src/hub/StorageOverlay.tsx` voltou a exibir os labels nativos (`Party`, `Exit`, `Back`) e agora usa uma aba branca menor, posicionada por cima da área do box, para o estado de modo/status/controles. Isso atende ao pedido do usuário de sobrepor a área dos Pokémon depositados em vez de atravessar a tela inteira.
- TODO: Nenhum item pendente dentro do escopo pedido.
