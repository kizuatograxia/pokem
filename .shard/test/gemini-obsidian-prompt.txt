Você é um assistente técnico de documentação para Obsidian.

Objetivo: atualizar EXAUSTIVAMENTE o vault do projeto pokém com base no estado atual real.

Instruções de saída (OBRIGATÓRIO):
1) Responda em Markdown.
2) Entregue exatamente estes blocos:
   - "## A) Nova nota de sessão" com conteúdo completo pronto para salvar em `obsidian-vault/07 Sessions/2026-04-01 OpenClaw-Gateway-and-Task-Orchestration.md`
   - "## B) Patch para Session Log" com um trecho curto para anexar em `obsidian-vault/07 Sessions/Session Log.md`
   - "## C) Patch para Near-Term Execution Plan" com novos bullets objetivos para `obsidian-vault/09 Backlog/Near-Term Execution Plan.md`
   - "## D) Patch para Risks and Unknowns" com riscos/mitigações curtos para `obsidian-vault/06 Operations/Risks and Unknowns.md`
3) Seja concreto, sem linguagem vaga.
4) Não invente fatos fora do contexto abaixo.

Contexto factual consolidado:
- Job cron `shard-watch` foi criado para rodar a cada 30s e processar `.shard/test/task-openclaw.md`.
- Depois foi desativado/removido; `openclaw cron list` retornou "No cron jobs".
- Houve instabilidade intermitente do gateway com erro:
  `gateway connect failed: Error: gateway closed (1000 normal closure): no close reason`
- `openclaw gateway status` e `openclaw status` mostraram comportamento inconsistente entre "RPC probe: ok" e timeouts de `openclaw gateway probe`.
- Configuração relevante em `C:\Users\hedge\.openclaw\openclaw.json`:
  - `gateway.mode = local`
  - `gateway.auth.mode = token`
  - havia `gateway.remote.url = ws://127.0.0.1:18789`; isso foi removido para evitar ambiguidade local/remote.
- `client/web` continua com trilha de trabalho ativa (UI pixel, PC/storage, Battle Tower), mas sem nova implementação nesta rodada — foco foi operação/orquestração.
- Usuário pediu para usar Gemini como apoio de delegação para documentação/tarefas.

Estado de backlog atual (Near-Term):
- inventário técnico da batalha
- inventário PBS
- estrutura do importador
- contrato inicial da batalha autoritativa
- higiene do workspace

Tom desejado:
- técnico, direto, orientado a execução
- português-BR, sem emojis
- incluir "proximos passos" e "criterios de pronto" quando fizer sentido
