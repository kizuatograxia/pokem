Original prompt: # Task: pixel-ui pixel-perfect — client/web

- Started pixel-perfect refactor for `client/web`.
- Scope limited to pixel UI components/screens requested in `.shard/test/task.md`.
- Intention: keep `HubGame` rendered in its existing fullscreen overlay so the 640x480 Phaser canvas is not clipped by the new 512x384 `PixelScreen`.
- Implemented `PixelScreen`, pixel-locked title/menu layout, sprite-backed dialogue box, and inline-style pixel frame/button updates.
- Added `src/components/ui-pixel/index.ts` barrel export for `PixelScreen`.
- Verification: `cd client/web && npx tsc --noEmit` passes.
- Extra check: `cd client/web && npm run build` reaches Vite config loading but fails with `spawn EPERM` in the environment, not a TypeScript source error.
- Playwright loop could not be run because `client/web` has no local Playwright install and network access is restricted.
- Follow-up worker task implemented the hub overlay flow inside `PixelScreen`: `HubGame` is now 512x384, owns the Phaser canvas plus React overlays, and `MainMenuScreen` renders it in-place instead of using a fullscreen fixed overlay.
- Added hub interaction events and overlays: NPC dialogue on `Z`/`Enter`, nurse yes/no flow, placeholder PC menu, typed `hub:*` CustomEvent helpers, and `dialogueOpen` exposure in `render_game_to_text`.
- Added minimal pixel UI primitives needed by the task: `PixelText` and `PixelMenuList`, and updated `PixelDialogueBox` to use the shared text primitive.
- Verification: `cd client/web && node_modules/.bin/tsc.cmd --noEmit` passes.
- Playwright skill check was attempted: the shared client script is present and executable, but a reliable local Vite+Playwright loop was not completed from this shell session, so no browser screenshot/state artifact was captured.
- Follow-up maintenance on 2026-03-31 finished the remaining Essentials asset sync for `client/web/public/assets/essentials`, adding the 32 files that were still missing after the Genesis mirror and previous selective imports.
- Gemini CLI recovery on 2026-03-31: trusted `C:\Users\hedge\OneDrive\Desktop\pokém` and `client\web` in `~/.gemini/trustedFolders.json`, replaced the global `~/.gemini/GEMINI.md` Lovable prompt with neutral defaults, removed the stale sessions directory, fixed the repo `GEMINI.md` Windows invocation, added the `System32` PATH workaround, and verified headless success with `gemini -m gemini-2.5-flash -p "OK"` from `client/web`.
- Follow-up on 2026-03-31 aligned the hub PC flow with Pokémon Essentials instead of the Genesis approximation. `client/web/src/hub/PcMenu.tsx` now matches the Essentials storage system menu (`Organize Boxes`, `Withdraw Pokemon`, `Deposit Pokemon`, `See ya!`), and `client/web/src/hub/StorageOverlay.tsx` was rebuilt around the real storage layout and interaction modes from `017_UI_PokemonStorage.rb` / `019_UI_PC.rb`.
- Added a stronger browser check for the PC flow at `http://127.0.0.1:4318`: menu opened from the PC tile, organize mode picked Venusaur, withdraw changed `Party: 3 -> 4`, deposit changed it back `4 -> 3`, and no console errors were reported. Artifacts are in `.shard/test/pc-storage-check.json` and `.shard/test/pc-storage-check.png`.
- Subsequent runtime check found the app itself was still booting correctly, but the storage preview pane could look broken because the selected Pokemon's front sprite was still loading. `client/web/src/hub/StorageOverlay.tsx` now falls back to an enlarged icon preview until the front sprite is ready, and the PC storage screenshot on `http://127.0.0.1:4319` no longer shows the blank pane.
- 2026-03-31: Segunda passada visual no PC/storage. `client/web/src/hub/StorageOverlay.tsx` foi retunado para reduzir o visual inflado do overlay: escala dos ícones menor, coordenadas do box alinhadas ao bitmap, preview menor, label de faixa do box no topo e ocultação dos ícones da party quando o painel está fechado. `client/web/src/hub/PcMenu.tsx` também foi compactado para uma janela mais contida.
- 2026-03-31: `cd client/web && node_modules/.bin/tsc.cmd --noEmit` passou após esse retoque.
- 2026-03-31: A checagem visual automatizada desta passada ficou pendente porque Vite/Playwright precisaram sair do sandbox (`spawn EPERM`) e o browser escalado não completou a execução. Funcionalidade do PC já estava confirmada antes; o que resta é conferir o acabamento no browser.
- 2026-03-31: Correção dirigida pelo screenshot do usuário. `client/web/src/components/ui-pixel/PixelMenuList.tsx` foi compactado para caber corretamente nas janelas pequenas, `client/web/src/hub/PcMenu.tsx` voltou a ter espaço suficiente para os quatro comandos, e `client/web/src/hub/StorageOverlay.tsx` moveu a ajuda/feedback para um painel dedicado, removendo a sobreposição feia com o painel esquerdo.
- 2026-03-31: Novo ajuste de modal na storage. A caixinha de ajuda do canto foi substituída por uma faixa `PixelFrame` de largura completa no rodapé em `client/web/src/hub/StorageOverlay.tsx`, para que `Deposit/Withdraw/Organize` tenham uma sobreposição visual mais total e não deixem a UI de baixo vazando.
- 2026-03-31: Ajuste seguinte após novo feedback visual. A faixa inferior inteira foi removida; `client/web/src/hub/StorageOverlay.tsx` agora recoloca `Party`, `Exit` e `Back` nos lugares nativos e usa uma aba branca menor sobre a área do box para exibir modo/status/controles.
- 2026-04-01: Passada visual no `Quick Battle` para corrigir spacing e composição da battle UI. O plugin Pencil/design foi tentado, mas o app não estava conectado (`WebSocket not connected to app: antigravity`), então o ajuste foi guiado por screenshots reais do browser em `.shard/test/quick-battle-check.png` e `.shard/test/quick-battle-fight.png`.
- 2026-04-01: `client/web/src/battle/BattleField.tsx` foi reequilibrado para recuperar o sprite do player que estava sendo coberto pelo fill inferior, reposicionar plataformas/sprites e melhorar o respiro geral do campo.
- 2026-04-01: `client/web/src/battle/BattleHud.tsx`, `client/web/src/battle/BattleMenu.tsx` e `client/web/src/battle/BattleScreen.tsx` foram retunados para melhorar prompt contextual, ancoragem dos databoxes e spacing dos menus de comando/moves. O menu de comando agora ocupa melhor a faixa inferior e o menu de golpes usa o painel direito só para o que cabe no asset.
- 2026-04-01: Criado `.shard/test/capture-quick-battle-state.cjs` para capturar separadamente os estados `command` e `fight` do Quick Battle e facilitar regressão visual rápida.
- 2026-04-01: Verificação desta passada: `cd client/web && npx tsc --noEmit` ✅, `node .shard/test/verify-quick-battle.cjs http://127.0.0.1:4317` ✅, `node .shard/test/capture-quick-battle-state.cjs http://127.0.0.1:4317 fight` ✅.
- 2026-04-01: Passada seguinte focada em asset import real da battle UI, sem UI inventada. Reusei o comportamento do `client/genesis/src/components/screens/BattleSceneScreen.tsx` e os sprites já presentes em `client/web/public/assets/sprites/ui/battle` / `client/web/public/assets/sprites/ui/shared`.
- 2026-04-01: `client/web/src/battle/BattleMenu.tsx` foi alinhado ao `genesis`: o menu de comando agora usa `overlay_command.png` como overlay único, o menu de golpes usa `cursor_fight.png` com `backgroundSize: 384px 874px` e offset real do spritesheet, e o badge de tipo usa `types.png` com `backgroundSize: 64px 532px`.
- 2026-04-01: Adicionado `.shard/test/web-game-actions-quick-battle.json` para dirigir o client compartilhado do skill `develop-web-game` até o estado `fight` do Quick Battle sem depender de JSON inline no `cmd`.
- 2026-04-01: Nova validação desta passada: `cd client/web && npx tsc --noEmit` ✅, `node .shard/test/verify-quick-battle.cjs http://127.0.0.1:4317` ✅, `node .shard/test/capture-quick-battle-state.cjs http://127.0.0.1:4317 fight` ✅, `node C:\Users\hedge\.codex\skills\develop-web-game\scripts\web_game_playwright_client.js --url http://127.0.0.1:4317 --actions-file .shard\test\web-game-actions-quick-battle.json --iterations 1 --pause-ms 500 --screenshot-dir .shard\test\web-game-skill` ✅.
- 2026-04-01: Artefatos confirmados visualmente: `.shard/test/quick-battle-check.png`, `.shard/test/quick-battle-fight.png` e `.shard/test/web-game-skill/shot-0.png`.
- 2026-04-01: TODO opcional para uma passada futura: expor `window.render_game_to_text` também no fluxo de batalha, porque o client compartilhado do skill ainda não gera `state-0.json` nessa tela.
